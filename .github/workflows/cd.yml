name: Publish NPM Package

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags:
      - v[1-9]+.[0-9]+.[0-9]+

jobs:
  test-build-publish:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: setup node ${{ matrix.node }}
        uses: actions/setup-node@v2
        with:
          node-version: "lts/*"
          registry-url: "https://registry.npmjs.org"

      - name: set git user
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com

      - name: update version
        run: npx standard-version

      - name: test
        run: |
          npm ci
          npm run test

      - name: build
        run: npm run build
        env:
          CI: false

      - name: create pull request
        uses: peter-evans/create-pull-request@v4
        with:
          commit-message: Update version and build artifacts
          title: Update version and build artifacts
          body: |
            This PR was created by the GitHub Action, **Update version and build artifacts**. 

            The commit hash is _${{ github.sha }}_.
          branch: update-version-and-build
          labels: |
            automated
            build
            version
          base: main

      - name: publish package
        run: |
          git push --follow-tags origin main
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
